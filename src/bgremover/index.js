import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import TopHeader from '../components/TopHeader';

const BGRemover = () => {
  return (
    <>
      <TopHeader titile={'BG Remover'} />
    </>
  );
};

export default BGRemover;

const styles = StyleSheet.create({});
